= WuWei データ仕様
三分一信之（三分一技術士事務所）
:revnumber: ver 2.0
:revdate: 2026-05-24
:revremark: draft for review
:version-label!:
:doctype: book
:toc:
:toclevels: 3
:sectnums:

== 本書の位置付け

本書は、`wu_wei2_2026-05-22.zip` に含まれる `app2/`、`cgi-bin/`、`server/` の最新実装に合わせて、WuWei の保存データ仕様を整理するものである。

現在の中心データモデルは `wuwei.note` v2 であり、主な正本は次の三つである。

[cols="1,2,3", options="header"]
|===
|区分 |主な場所 |役割

|アップロード実体
|`data/{user_uuid}/upload/YYYY/MM/DD/{file_uuid}/`
|元ファイル、サムネイル、Office 変換 PDF、`manifest.json` などの実体ファイルを保存する。

|リソース定義
|`data/{user_uuid}/resource/YYYY/MM/DD/{resource_uuid}/resource.json`
|ホーム画面、リソース一覧、検索で使用する Resource library の正本メタデータ。実体ファイルは `storage.files[]` で `upload/` 側を参照する。

|ノート
|`data/{user_uuid}/note/YYYY/MM/DD/{note_uuid}/note.json`
|ノート JSON の正本。ページ、ノード、リンク、グループ、Content node 内の compact resource、共同作業メタデータなどを保持する。
|===

NOTE: 旧仕様では `note.txt` に Base64 JSON を保存していたが、現在の保存 API は `note.json` を保存する。旧形式は読み込み時の変換対象であり、`wuwei.note.v0` / `wuwei.note.v1` / `wuwei.note.v2` の正規化段階で v2 形式へ変換する。正規化後の通常処理では旧項目への fallback を行わない。

=== 保存データと実行時描画項目の分離

Node の表示スタイルは、保存データでは `style` を正本とする。`style.fill`、`style.line`、`style.font`、`style.label` が、色、線、フォント及びラベル表示の保存対象項目である。

一方、`wuwei.draw` の SVG 描画コードでは、可読性及び simulation 描画時の応答性を考慮して、実行時に `node.color`、`node.outline`、`node.outlineWidth`、`node.font` を参照できる。これらは load / normalize / graph 展開時に `style` から展開される runtime mirror field であり、保存対象ではない。

実装では、これらの runtime mirror field は non-enumerable property として定義する。したがって、`JSON.stringify()` や `util.clone()` による保存用コピーには含まれない。旧データ又は旧処理で enumerable な `color`、`outline`、`outlineWidth`、`font` が存在する場合、保存前に `style` へ補完した上で削除する。

[source,text]
----
保存正本:
  node.style.fill
  node.style.line.color
  node.style.line.width
  node.style.font.*

実行時描画用 mirror field:
  node.color        = node.style.fill
  node.outline      = node.style.line.color
  node.outlineWidth = node.style.line.width
  node.font         = node.style.font から SVG 用に展開
----

`current.page` は保存正本であり、`graph.nodes[]` は SVG 描画用の runtime view model である。SVG 描画性能を確保するため、`graph.nodes[]` には描画、選択、ドラッグ、簡易表示に必要な軽量項目のみを保持する。

`description.body` は保存正本の本文又は説明である。Memo では表示本文であり、Topic / Content では図形内の説明文として使用できる。描画時には `description.format` に従って HTML を生成し、SVG の `foreignObject` 内に描画する。したがって、`description.body` の全文又は HTML 変換結果を保存正本とは別に保存しない。必要な場合、graph 側には実行時キャッシュとして `descriptionHtml` 又は同等の描画用値を持たせることができるが、保存対象とはしない。

[cols="1,2,3", options="header"]
|===
|データ |役割 |保持する主な内容

|`current.page.nodes[]`
|保存正本
|`id`, `type`, `label`, `description`, `x`, `y`, `visible`, `shape`, `size`, `style`, `resource`, `time`, `pageNumber`, `anchorHref`, `audit`

|`graph.nodes[]`
|SVG 描画用 runtime view model
|`id`, `type`, `x`, `y`, `visible`, `state`, `label`, `description`, `shape`, `size`, `style`, runtime mirror field, `iconKind`, `thumbnailUrl`

|`graph.links[]`
|SVG 描画用 link view model
|`id`, `source`, `target`, `type`, `role`, `style`, `state`
|===

`graph.nodes[]` には、`resource.original`、`resource.storage.files[]`、`resource.viewer`、`resource.media`、`resource.rights.*`、`audit` などの重い内容属性を原則として保持しない。表示ペイン、編集ペイン、管理者用ペインは `current.page` の保存正本を参照する。

`x`、`y`、`state`、`visible`、`linkCount`、`shape`、`size` は当面 Node 直下の保存項目として扱う。特に `x` 及び `y` は D3 simulation / drag 処理との親和性が高いため、`canvas.position` などの深い構造には移動しない。
== 用語と識別子

=== 識別子

[cols="1,3", options="header"]
|===
|項目 |説明

|`note_id` / `note_uuid`
|ノートの識別子。現在の実装では `note_id` が主項目であり、保存側で互換用に `note_uuid` も補完する。

|`page.id`
|ページ識別子。

|`node.id`
|ノード識別子。SVG / DOM ID として使われるため、現在の `util.createUuid()` は `"_" + uuid.v4()` 形式を返す。

|`link.id`
|リンク識別子。ノードと同様に `"_" + uuid.v4()` 形式を基本とする。

|`group.id`
|グループ識別子。ノード、リンクと同様に内部 ID として扱う。

|`resource.id`
|リソース識別子。アップロード bundle の UUID と対応する場合がある。

|`team_id`
|共同作業チーム識別子。設計上は `"t-" + uuid.v4()` 形式とする。
|===

=== note_key

`note_key` は、ノートルートから見た相対パスである。`list-note` の応答では、同じ `note_id` を持つノートを区別するため、`id` だけでなく `note_key` を使用する。

例:

[source,text]
----
2026/05/21/_5c1f594a-315b-4bab-9075-62b1bebed7eb
----

`load-note`、`remove-note`、`export-note` では、可能な限り `note_key` を使って対象ノートを指定する。

== ディレクトリ構成

=== 個人ノート領域

[source,text]
----
data/{user_uuid}/
  upload/
    YYYY/MM/DD/{file_uuid}/
      manifest.json
      {original_file}
      thumbnail.jpg
      preview.pdf
  resource/
    YYYY/MM/DD/{resource_uuid}/
      resource.json
  note/
    YYYY/MM/DD/{note_uuid}/
      note.json
      resource/
  trash/
----

=== チーム共同作業ノート領域

チーム共同作業ノートは、個人ノートとは別に、チーム ID 配下へ保存する。

[source,text]
----
data/{team_id}/YYYY/MM/DD/{note_uuid}/section/*
----

`team_id` は説明上の `collabo-teamX` ではなく、実際には次の形式で生成する。

[source,javascript]
----
team_id = "t-" + uuid.v4();
----

例:

[source,text]
----
data/t-5c1f594a-315b-4bab-9075-62b1bebed7eb/2026/05/21/_note_uuid/section/*
----

=== サーバー管理テーブル領域

チーム、利用者、所属関係は、初期実装ではサーバー側 JSON テーブルとして管理できる。

[source,text]
----
data/admin/
  teams.json
  team-members.json
  users.json
----

DB 化する場合でも、論理項目は同じとする。

== Upload bundle

アップロードされた実体ファイルは、日付別の `upload/` ディレクトリに保存する。

[source,text]
----
upload/YYYY/MM/DD/{file_uuid}/
  manifest.json
  {original_file}
  thumbnail.jpg
  preview.pdf
----

`manifest.json` はアップロード単位のマニフェストであり、元ファイル、サムネイル、プレビュー PDF などの実体ファイルを記録する。

PDF の場合は、元ファイルが文書表示対象になる。Office 文書の場合は、LibreOffice などで `preview.pdf` を生成し、表示やページ軸にはこの PDF を使用する。

== Resource library entry

Resource library の正本は、次の場所に保存される。

[source,text]
----
resource/YYYY/MM/DD/{resource_uuid}/resource.json
----

`resource.json` は、ホーム画面、リソース一覧、検索、Content node 作成の入力として使用する。

[[resource-v2-basic]]
=== Resource の基本形

Resource は、Content node が参照するコンテンツ実体及びその管理情報を表す。

Resource library の `resource.json` と、Content node 内に保存する compact resource は、同一の v2 Resource 構造を基本とする。Content node 側では、この Resource を `node.resource` として埋め込む。

Resource には、利用者が入力又は補正できるメタデータだけでなく、サーバーがファイルを受け付けて登録した際に取得又は生成した管理情報も含まれる。サーバー登録情報には、保存パス、ファイル名、MIME type、ファイルサイズ、ハッシュ値、thumbnail、preview などの生成ファイル情報が含まれる。

これらは Content node の resource データとして保存するが、サーバー登録又は生成により確定した情報は利用者による通常編集対象とはしない。編集不可情報は編集ペインでは変更できない。ただし、情報ペイン及び管理者用ペインでは確認できるように表示する。

==== Resource の基本分類

`resource.source` は取得元又は保管方式を表す。`resource.kind` は Content 種別を表す。両者は混同しない。例えば、利用者がアップロードした mp3 ファイルでは、`resource.source` は `upload`、`resource.kind` は `audio` とする。利用者がアップロードした画像ファイルでは、`resource.source` は `upload`、`resource.kind` は `image` とする。`resource.kind` に `upload` は使用しない。

[cols="25%,75%",options="header"]
|===
|分類 |意味

|`source`
|`upload`、`remote`、`generated`、`embedded` などの取得元又は保管方式

|`kind`
|`document`、`html`、`video`、`image`、`audio`、`other` などの Content 種別

|`documentKind`
|`pdf`、`office`、`text` など。HTML は `kind: "html"` として独立させる。

|`videoKind`
|`youtube`、`vimeo`、`mp4` など

|`audioKind`
|`mp3`、`m4a`、`wav` など

|`imageKind`
|`png`、`jpeg`、`gif`、`webp`、`svg` など
|===

==== URI / URL の基本方針

新規 v2 Resource では、トップレベルの `resource.uri`、`resource.canonicalUri`、`resource.title`、`resource.thumbnailUri` は定義しない。

Content の表示名は `node.label` に一本化する。アップロード時の初期表示名は、`resource.storage.files[role="original"].file_name` から設定してよい。以後は `node.label` を利用者が編集する。

upload Content の原本ファイルは、`resource.original.storageRole` と `resource.storage.files[]` により管理する。remote Content の外部参照先は、`resource.original.url` に保存する。外部資料の正規 URL は `resource.original.canonicalUrl` に保存し、DOI、NDL、ISBN、URN などの永続識別子は `resource.original.identifiers[]` に保存する。

[cols="1,1,3", options="header"]
|===
|対象 |項目 |意味

|upload Content
|`resource.original.storageRole`
|原本ファイルを示す `storage.files[]` の role。通常は `original`。

|upload Content
|`resource.storage.files[]`
|original、thumbnail、preview、pdf-preview、waveform などの登録済み又は生成済みファイル情報。

|remote Content
|`resource.original.url`
|登録時又は現在参照する外部 URL。remote Content の定義時入力項目であり、編集可能とする。

|remote Content
|`resource.original.canonicalUrl`
|外部資料の正規 URL。remote Content の定義時入力項目であり、編集可能とする。

|remote Content
|`resource.original.identifiers[]`
|DOI、NDL、ISBN、URN、YouTube ID、Vimeo ID などの外部識別子。
|===

内部ファイルの場合、保存値には `/wu_wei2/server/load-file.cgi`、`/wu_wei2/cgi-bin/load-file.py`、`user_id`、利用者 UUID、ホスト名を含む実行時アクセス URL を保存しない。保存値は、`YYYY/MM/DD/file_uuid/filename` 形式の論理パスとする。

アップロードされた文書、画像、音声、動画を表示又は再生する場合は、保存済みの論理パスから、実行時に `load-file.cgi` 又は `load-file.py` 経由のアクセス URL を生成する。生成した実行時 URL を保存データへ書き戻してはならない。

==== Resource 基本 JSON 例

[source,json]
----
{
  "type": "Resource",
  "id": "949127a0-3f14-4ba6-9239-c471334fdbba",
  "source": "upload",
  "kind": "document",
  "documentKind": "pdf",
  "videoKind": "",
  "audioKind": "",
  "imageKind": "",
  "mimeType": "application/pdf",

  "original": {
    "type": "upload",
    "storageRole": "original"
  },

  "media": {
    "kind": "document",
    "mimeType": "application/pdf",
    "downloadable": true,
    "pageCount": 27
  },

  "contents": {
    "type": "pdf",
    "axis": {
      "unit": "page",
      "nodeType": "page"
    },
    "pageCount": 27,
    "hasPageCount": true,
    "pageOffset": 0,
    "firstPageNumber": 1,
    "sourceRole": "original"
  },

  "storage": {
    "managed": true,
    "copyPolicy": "reference",
    "manifest": {
      "area": "upload",
      "path": "2026/05/21/949127a0-3f14-4ba6-9239-c471334fdbba/manifest.json"
    },
    "files": [
      {
        "role": "original",
        "area": "upload",
        "path": "2026/05/21/949127a0-3f14-4ba6-9239-c471334fdbba/document.pdf",
        "dir_name": "data/{user_uuid}/upload/2026/05/21/949127a0-3f14-4ba6-9239-c471334fdbba/",
        "file_name": "document.pdf",
        "mimeType": "application/pdf",
        "size": 1234567,
        "sha256": "",
        "registeredAt": "2026-05-21T10:00:00+09:00",
        "registeredBy": "user_uuid"
      },
      {
        "role": "thumbnail",
        "area": "upload",
        "path": "2026/05/21/949127a0-3f14-4ba6-9239-c471334fdbba/thumbnail.jpg",
        "dir_name": "data/{user_uuid}/upload/2026/05/21/949127a0-3f14-4ba6-9239-c471334fdbba/",
        "file_name": "thumbnail.jpg",
        "mimeType": "image/jpeg",
        "size": 12345,
        "sha256": "",
        "generated": true,
        "registeredAt": "2026-05-21T10:00:00+09:00",
        "registeredBy": "server"
      }
    ]
  },

  "viewer": {
    "defaultMode": "pdfjs",
    "supportedModes": ["pdfjs", "newTab", "download"],
    "embed": {
      "enabled": true,
      "sourceRole": "original"
    },
    "thumbnailRole": "thumbnail"
  },

  "rights": {
    "owner": "",
    "copyright": "",
    "license": "",
    "attribution": ""
  },

  "state": "active",
  "audit": {}
}
----

`viewer` は表示方法の判定に必要な補助情報を保持する。表示対象 URL や thumbnail URL は、`resource.original` 又は `resource.storage.files[]` から実行時に解決する。

=== Resource 種別別の例

Resource は、Document、HTML、Video、Audio、Image で共通構造を使用するが、差異は主に `resource.source`、`resource.kind`、`resource.documentKind` / `resource.videoKind` / `resource.audioKind` / `resource.imageKind`、`resource.original`、`resource.media`、`resource.viewpoint`、`resource.storage.files[]`、`resource.viewer` に現れる。

==== Document PDF

[source,json]
----
{
  "source": "upload",
  "kind": "document",
  "documentKind": "pdf",
  "mimeType": "application/pdf",
  "original": {
    "type": "upload",
    "storageRole": "original"
  },
  "media": {
    "kind": "document",
    "mimeType": "application/pdf",
    "downloadable": true,
    "pageCount": 27
  },
  "contents": {
    "type": "pdf",
    "axis": { "unit": "page", "nodeType": "page" },
    "pageCount": 27,
    "hasPageCount": true,
    "pageOffset": 0,
    "firstPageNumber": 1,
    "sourceRole": "original"
  },
  "storage": {
    "files": [
      {
        "role": "original",
        "area": "upload",
        "path": "2026/05/21/949127a0-3f14-4ba6-9239-c471334fdbba/document.pdf",
        "file_name": "document.pdf",
        "mimeType": "application/pdf"
      },
      {
        "role": "thumbnail",
        "area": "upload",
        "path": "2026/05/21/949127a0-3f14-4ba6-9239-c471334fdbba/thumbnail.jpg",
        "file_name": "thumbnail.jpg",
        "mimeType": "image/jpeg",
        "generated": true
      }
    ]
  },
  "viewer": {
    "defaultMode": "pdfjs",
    "supportedModes": ["pdfjs", "newTab", "download"],
    "embed": {
      "enabled": true,
      "sourceRole": "original"
    },
    "thumbnailRole": "thumbnail"
  }
}
----

==== Document Office

Office 文書の原本は `storage.files[role="original"]` に保持する。Office 文書を PDF preview へ変換した場合、変換後ファイルは `storage.files[role="pdf-preview"]` に保持する。

[source,json]
----
{
  "source": "upload",
  "kind": "document",
  "documentKind": "office",
  "mimeType": "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
  "original": {
    "type": "upload",
    "storageRole": "original"
  },
  "media": {
    "kind": "document",
    "mimeType": "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
    "downloadable": true,
    "pageCount": 12
  },
  "storage": {
    "files": [
      {
        "role": "original",
        "area": "upload",
        "path": "2026/05/21/949127a0-3f14-4ba6-9239-c471334fdbba/report.docx",
        "file_name": "report.docx"
      },
      {
        "role": "pdf-preview",
        "area": "upload",
        "path": "2026/05/21/949127a0-3f14-4ba6-9239-c471334fdbba/report.pdf",
        "file_name": "report.pdf",
        "generated": true
      },
      {
        "role": "thumbnail",
        "area": "upload",
        "path": "2026/05/21/949127a0-3f14-4ba6-9239-c471334fdbba/thumbnail.jpg",
        "file_name": "thumbnail.jpg",
        "generated": true
      }
    ]
  },
  "viewer": {
    "defaultMode": "pdfPreview",
    "supportedModes": ["pdfPreview", "officeViewer", "download"],
    "embed": {
      "enabled": true,
      "sourceRole": "pdf-preview"
    },
    "thumbnailRole": "thumbnail"
  }
}
----

Office 文書では、`resource.original.storageRole` は元ファイルを示し、`viewer.embed.sourceRole` は表示用 PDF preview を示すことがある。EC2 サーバー上で外部 Office viewer を使用する場合でも、保存データには実行時 viewer URL ではなく元ファイル又は preview の論理パスを保持し、表示時に viewer URL を生成する。

==== HTML

HTML 文書又は Web ページは `kind: "html"` とする。外部 Web ページの場合、`source` は `remote` とし、外部参照情報は `resource.original` に保持する。HTML Viewpoint はページ番号ではなくアンカーを扱う。

[source,json]
----
{
  "source": "remote",
  "kind": "html",
  "mimeType": "text/html",
  "original": {
    "type": "remote",
    "url": "https://example.com/article.html",
    "canonicalUrl": "https://example.com/article.html",
    "accessedAt": "2026-05-24T10:00:00+09:00",
    "identifiers": []
  },
  "media": {
    "kind": "html",
    "mimeType": "text/html",
    "downloadable": false
  },
  "viewer": {
    "defaultMode": "newTab",
    "supportedModes": ["newTab", "iframe"],
    "embed": {
      "enabled": true
    }
  },
  "rights": {
    "owner": "",
    "copyright": "",
    "license": "",
    "attribution": ""
  }
}
----

==== Video YouTube

[source,json]
----
{
  "source": "remote",
  "kind": "video",
  "videoKind": "youtube",
  "mimeType": "text/html",
  "original": {
    "type": "remote",
    "url": "https://www.youtube.com/watch?v=VIDEO_ID",
    "canonicalUrl": "https://www.youtube.com/watch?v=VIDEO_ID",
    "accessedAt": "2026-05-24T10:00:00+09:00",
    "identifiers": [
      { "scheme": "youtube", "value": "VIDEO_ID" }
    ]
  },
  "media": {
    "kind": "video",
    "provider": "youtube",
    "videoId": "VIDEO_ID",
    "durationSeconds": 3600
  },
  "viewer": {
    "defaultMode": "youtube",
    "supportedModes": ["youtube", "newTab"],
    "embed": {
      "enabled": true,
      "provider": "youtube"
    }
  }
}
----

動画の区間指定は、Resource の `contents` ではなく Timeline group と Segment node で扱う。Resource 側には、必要に応じて `media.durationSeconds`、`media.provider`、`media.videoId` などの補助情報を保持する。

==== Video Vimeo

[source,json]
----
{
  "source": "remote",
  "kind": "video",
  "videoKind": "vimeo",
  "mimeType": "text/html",
  "original": {
    "type": "remote",
    "url": "https://vimeo.com/933802755",
    "canonicalUrl": "https://vimeo.com/933802755",
    "accessedAt": "2026-05-24T10:00:00+09:00",
    "identifiers": [
      { "scheme": "vimeo", "value": "933802755" }
    ]
  },
  "media": {
    "kind": "video",
    "provider": "vimeo",
    "videoId": "933802755",
    "durationSeconds": 1200
  },
  "viewer": {
    "defaultMode": "vimeo",
    "supportedModes": ["vimeo", "newTab"],
    "embed": {
      "enabled": true,
      "provider": "vimeo"
    }
  }
}
----

==== Video file

[source,json]
----
{
  "source": "upload",
  "kind": "video",
  "videoKind": "mp4",
  "mimeType": "video/mp4",
  "original": {
    "type": "upload",
    "storageRole": "original"
  },
  "media": {
    "kind": "video",
    "mimeType": "video/mp4",
    "durationSeconds": 600
  },
  "storage": {
    "files": [
      {
        "role": "original",
        "area": "upload",
        "path": "2026/05/21/949127a0-3f14-4ba6-9239-c471334fdbba/movie.mp4",
        "file_name": "movie.mp4"
      },
      {
        "role": "thumbnail",
        "area": "upload",
        "path": "2026/05/21/949127a0-3f14-4ba6-9239-c471334fdbba/movie-thumbnail.jpg",
        "file_name": "movie-thumbnail.jpg",
        "generated": true
      }
    ]
  },
  "viewer": {
    "defaultMode": "video",
    "supportedModes": ["video", "newTab", "download"],
    "embed": {
      "enabled": true,
      "sourceRole": "original"
    },
    "thumbnailRole": "thumbnail"
  }
}
----

==== Audio

[source,json]
----
{
  "source": "upload",
  "kind": "audio",
  "audioKind": "mp3",
  "mimeType": "audio/mpeg",
  "original": {
    "type": "upload",
    "storageRole": "original"
  },
  "media": {
    "kind": "audio",
    "mimeType": "audio/mpeg",
    "durationSeconds": 300
  },
  "storage": {
    "files": [
      {
        "role": "original",
        "area": "upload",
        "path": "2026/05/21/949127a0-3f14-4ba6-9239-c471334fdbba/audio.mp3",
        "file_name": "audio.mp3"
      }
    ]
  },
  "viewer": {
    "defaultMode": "audio",
    "supportedModes": ["audio", "download"],
    "embed": {
      "enabled": true,
      "sourceRole": "original"
    }
  }
}
----

Audio も時間軸上の注釈対象にできるが、区間指定は Resource の `contents` ではなく Timeline group と Segment node で扱う。

==== Image

[source,json]
----
{
  "source": "upload",
  "kind": "image",
  "imageKind": "jpeg",
  "mimeType": "image/jpeg",
  "original": {
    "type": "upload",
    "storageRole": "original"
  },
  "media": {
    "kind": "image",
    "mimeType": "image/jpeg",
    "width": 1600,
    "height": 900
  },
  "storage": {
    "files": [
      {
        "role": "original",
        "area": "upload",
        "path": "2026/05/21/949127a0-3f14-4ba6-9239-c471334fdbba/photo.jpg",
        "file_name": "photo.jpg"
      },
      {
        "role": "thumbnail",
        "area": "upload",
        "path": "2026/05/21/949127a0-3f14-4ba6-9239-c471334fdbba/photo-thumbnail.jpg",
        "file_name": "photo-thumbnail.jpg",
        "generated": true
      }
    ]
  },
  "viewer": {
    "defaultMode": "image",
    "supportedModes": ["image", "newTab", "download"],
    "embed": {
      "enabled": true,
      "sourceRole": "original"
    },
    "thumbnailRole": "thumbnail"
  }
}
----

Image は `resource.viewpoint` を使用しない。画像表示に必要な URL は、保存された論理パスから実行時に生成する。実行時 URL を保存データへ書き戻してはならない。

==== Resource 種別ごとの差異

[cols="1,2,2,2,3", options="header"]
|===
|種別
|識別項目
|contents
|storage role
|viewer

|PDF
|`kind=document`, `documentKind=pdf`
|`axis.unit=page`
|`original`, `thumbnail`
|`original` を PDF.js 等で表示する。

|Office
|`kind=document`, `documentKind=office`
|必要に応じて `axis.unit=page`
|`original`, `pdf-preview`, `thumbnail`
|ローカルでは変換 PDF preview、EC2 では必要に応じて外部 Office viewer を使用する。

|HTML
|`kind=html`
|ページ番号ではなく anchor を PageMarker 側で扱う。
|remote では通常なし。upload HTML では `original`, 必要に応じて `thumbnail`
|外部 URL 又は upload HTML を newTab / iframe で表示する。

|YouTube / Vimeo
|`kind=video`, `videoKind=youtube` / `vimeo`
|使用しない。時間軸は Timeline group で扱う。
|通常なし。
|`resource.original.url` から player URL を実行時に生成する。

|MP4
|`kind=video`, `videoKind=mp4`
|使用しない。時間軸は Timeline group で扱う。
|`original`, `thumbnail`
|保存された論理パスから実行時 URL を生成して video 表示する。

|Audio
|`kind=audio`, `audioKind=mp3`
|使用しない。時間軸は Timeline group で扱う。
|`original`, 必要に応じて `thumbnail` 又は `waveform`
|保存された論理パスから実行時 URL を生成して audio 表示する。

|Image
|`kind=image`, `imageKind=jpeg` など
|使用しない。
|`original`, 必要に応じて `thumbnail`
|保存された論理パスから実行時 URL を生成して image 表示する。
|===

=== Resource 情報の編集可否

Resource に含まれる情報は、更新可情報と参照情報に分けて扱う。

[cols="1,1,3", options="header"]
|===
|項目
|更新可 / 参照
|説明

|`node.label`
|更新可
|Content node の表示名。Resource 側には `title` を持たせず、表示名は `node.label` に一本化する。

|`resource.source`
|参照
|`upload`, `remote`, `generated`, `embedded` など。登録又は判定時に確定する。

|`resource.kind`
|参照
|`document`, `html`, `video`, `image`, `audio`, `other`。登録又は判定時に確定する。

|`resource.documentKind`
|参照
|PDF / Office / text など。登録又は判定時に確定する。

|`resource.videoKind`
|参照
|YouTube / Vimeo / mp4 など。登録又は判定時に確定する。

|`resource.audioKind`
|参照
|mp3 / m4a / wav など。登録又は判定時に確定する。

|`resource.imageKind`
|参照
|png / jpeg / gif / webp / svg など。登録又は判定時に確定する。

|`resource.mimeType`
|参照
|サーバー登録時又は判定時に取得した MIME type。

|`resource.original.type`
|参照
|`upload`、`remote` などの原本種別。

|`resource.original.storageRole`
|参照
|upload Content の原本ファイル role。通常は `original`。

|`resource.original.url`
|更新可
|remote Content の登録時又は現在参照する URL。remote Content 定義時の入力項目である。

|`resource.original.canonicalUrl`
|更新可
|remote Content の正規 URL。remote Content 定義時の入力項目である。

|`resource.original.accessedAt`
|参照
|remote Content の登録又は取得日時。

|`resource.original.identifiers[]`
|更新可
|DOI、NDL、ISBN、URN、YouTube ID、Vimeo ID などの外部識別子。

|`resource.media`
|参照
|プログラムが判定・利用するメディア補助情報。

|`resource.storage.files[]`
|参照
|サーバーが登録・生成した実体ファイル情報。保存パス、ファイル名、MIME type、サイズ、ハッシュ値、生成有無等を含む。

|`resource.viewer`
|参照
|プログラムが表示方法を決定するための情報。通常利用者の編集対象にはしない。

|`resource.viewpoint.firstPageNumber`
|更新可
|対象ファイルの物理第 1 ページに対応する元文書上のページ番号。元文書の一部を抽出した PDF 等では、この値だけを利用者が編集する。

|`resource.viewpoint.pageOffset`
|参照
|`firstPageNumber - 1` により算出する派生値。

|`resource.viewpoint.pageCount`
|参照
|文書の物理ページ数。`resource.media.pageCount` と同じ値として扱う。

|`resource.rights.owner`
|更新可
|権利者、所有者、著作者、提供元など。

|`resource.rights.copyright`
|更新可
|著作権表示。

|`resource.rights.license`
|更新可
|ライセンス名又は利用条件。

|`resource.rights.attribution`
|更新可
|表示すべき帰属・クレジット文。

|`resource.state`
|参照
|active / deleted などの状態。
|===

`resource.rights.*` は Content の典拠・権利タブで編集する。graph には反映しない。

=== documentKind

`kind` が `document` の場合は、`documentKind` で文書種別を表す。

[cols="1,3", options="header"]
|===
|documentKind |意味

|`pdf`
|PDF 文書。

|`office`
|Word、Excel、PowerPoint などの Office 文書。

|`text`
|テキスト文書。
|===

=== videoKind

`kind` が `video` の場合は、`videoKind` で動画種別を表す。

[cols="1,3", options="header"]
|===
|videoKind |意味

|`youtube`
|YouTube 動画。

|`vimeo`
|Vimeo 動画。

|`mp4`
|アップロード済み MP4 動画ファイル。
|===

=== audioKind

`kind` が `audio` の場合は、`audioKind` で音声種別を表す。

[cols="1,3", options="header"]
|===
|audioKind |意味

|`mp3`
|mp3 音声ファイル。

|`m4a`
|m4a / AAC 音声ファイル。

|`wav`
|WAV 音声ファイル。
|===

`kind` が `image` の場合は、必要に応じて `imageKind` を使用する。

[cols="1,3", options="header"]
|===
|imageKind |意味

|`png`
|PNG 画像ファイル。

|`jpeg`
|JPEG 画像ファイル。`jpg` 拡張子もこの値に正規化できる。

|`gif`
|GIF 画像ファイル。

|`webp`
|WebP 画像ファイル。

|`svg`
|SVG 画像ファイル。
|===

=== 旧項目及び fallback の扱い

v2 仕様では、Resource 情報は本節の構造に統一する。

旧形式由来の `resourceRef`、node 直下の `uri` / `url` / `format` / `thumbnail`、`resource.kind: "upload"`、`resource.file`、`resource.date`、旧 `resource.uri`、旧 `resource.canonicalUri`、旧 `resource.title`、旧 `resource.thumbnailUri` などは、読み込み時の正規化段階で v2 Resource に変換する。正規化後の `wuwei.model`、`wuwei.edit`、`wuwei.info`、`wuwei.draw` などの通常処理では、これら旧項目を参照する fallback 処理を行わない。
== load-file による参照

クライアントは、`data/` 配下のファイルを直接参照せず、原則として `load-file.cgi` 又は同等の公開 URL 生成関数を経由する。

例:

[source,text]
----
/wu_wei2/server/load-file.cgi?area=upload&path=2026%2F05%2F21%2F_file_uuid%2Fthumbnail.jpg&user_id={user_uuid}
----

`area` は、現在次の値を想定する。

* `upload`
* `note`
* `resource`
* `thumbnail`
* `content`

`path` はユーザー領域内の相対パスであり、`..` や絶対パスは許可しない。

== Note file

現在の保存 API は、ノートを `note.json` として保存する。

[source,text]
----
data/{user_uuid}/note/YYYY/MM/DD/{note_uuid}/note.json
----

`note.json` は、JSON オブジェクトを UTF-8 で保存する。旧 `note.txt` 形式の Base64 包装は、読み込み互換の対象である。

== Note JSON

=== 基本形

[source,json]
----
{
  "dataModel": {
    "name": "wuwei.note",
    "version": "v2"
  },
  "note_id": "_note_uuid",
  "note_uuid": "_note_uuid",
  "note_name": "Note title",
  "description": "Note description",
  "thumbnail": "<svg ...>",
  "currentPage": "_page_uuid",
  "resources": [],
  "pages": [],
  "collaboration": {
    "enabled": false,
    "revision": 0,
    "updatedAt": ""
  },
  "exchange": {
    "imported": false,
    "mode": "",
    "source": "",
    "importedBy": "",
    "importedAt": ""
  },
  "collabNoteState": "own",
  "note_scope": "personal",
  "team_id": "",
  "audit": {
    "owner": "guest",
    "createdBy": "user_uuid",
    "createdAt": "2026-05-21T10:00:00+09:00",
    "lastModifiedBy": "",
    "lastModifiedAt": ""
  }
}
----

=== collabNoteState

`wuwei.collab` 側のノート状態判定は、次の三値に統一する。

[cols="1,3", options="header"]
|===
|値 |意味

|`own`
|自分で作ったノート。

|`imported`
|他者から入手して import したノート。

|`team`
|チームの共同編集ノート。
|===

`note_scope` は保存領域を示す補助項目である。

[cols="1,3", options="header"]
|===
|値 |意味

|`personal`
|個人ノート領域。

|`team`
|チーム共同作業ノート領域。
|===

`team_id` は、`collabNoteState` が `team` の場合に使用する。

=== collaboration

共同作業用のメタデータである。

[cols="1,3", options="header"]
|===
|項目 |意味

|`enabled`
|共同作業同期を有効にするか。

|`revision`
|サーバー側で管理するノート revision。

|`updatedAt`
|最新 revision 更新日時。
|===

=== exchange

export / import によって入手したノートの情報を保持する。

[cols="1,3", options="header"]
|===
|項目 |意味

|`imported`
|import 済みノートか。

|`mode`
|`imported` などの交換モード。

|`source`
|`import` などの由来。

|`importedBy`
|import 処理を行った利用者。

|`importedAt`
|import 処理日時。
|===

== Page

Page は、ノート内の編集キャンバス単位である。

[source,json]
----
{
  "id": "_page_uuid",
  "pp": 1,
  "name": "",
  "description": "",
  "nodes": [],
  "links": [],
  "groups": [],
  "transform": {
    "x": 0,
    "y": 0,
    "scale": 1
  },
  "thumbnail": "<svg ...>",
  "audit": {}
}
----

`common.current` が現在編集中の Note JSON であり、常に保存対象となる正本である。`common.current.page` は現在表示中の Page を参照し、その `nodes`、`links`、`groups`、`transform` が保存対象となる。`common.current.page` は `common.current.pages[]` 内の選択 Page オブジェクトそのものへの参照であるため、`common.current.page.nodes[]` への変更は `common.current.pages[]` 内の当該 Page にも反映される。現在のコードでは `common.current.currentPage` は配列添字ではなく Page の `id` を保持する。`common.graph` は、`common.current.page` から描画用に展開された作業データであり、保存正本ではない。

[NOTE]
====
`common.current.page` は、`common.current.pages[]` から `common.current.currentPage` と一致する `id` を持つ Page オブジェクトを取得し、その Page オブジェクトそのものへの参照として設定する。したがって、実装では次のように `find()` を使用する。

[source,javascript]
----
common.current.page = common.current.pages.find(function (page) {
  return page && page.id === common.current.currentPage;
}) || null;
----

`common.current.pages.filter(...)` は配列を返すため、`common.current.page` への設定には使用しない。また、`common.current.currentPage` は配列添字ではなく Page の `id` であるため、`common.current.pages[common.current.currentPage]` のような配列添字参照も使用しない。
====

== common.current と common.graph

ノート読込時には、保存された Note JSON を `common.current` に復元し、選択中の `common.current.page` から描画用の `common.graph` を構成する。

[source,text]
----
note.json
  ↓
common.current
  ↓
common.current.page.nodes
common.current.page.links
common.current.page.groups から生成した pseudo nodes / pseudo links
  ↓
common.graph
----

保存時も `common.graph.nodes` / `common.graph.links` から `common.current.page.nodes` / `common.current.page.links` への逆方向変換は行わない。この目的の互換関数や同期関数も定義しない。

[source,text]
----
ユーザ操作・編集
  ↓
common.current.page.nodes
common.current.page.links
common.current.page.groups
common.current.page.transform
  ↓
common.current.pages[] を note.json として保存
----

`common.graph.nodes` と `common.graph.links` は、`common.current.page` から展開された描画用配列である。実ノード及び実リンクは `common.current.page.nodes` / `common.current.page.links` と同じオブジェクトを参照するため、ノートキャンバス上で変更される `x`、`y`、`visible` などの保存対象項目は `common.current.page` 側の正本に反映される。したがって、`common.graph` から `common.current.page` へ戻す処理は不要であり、実装しない。`pseudo node` / `pseudo link` は `groups[]` 等から実行時に生成される描画要素であり、`nodes[]` / `links[]` には保存しない。

ノード及びリンクの位置、並びに表示 / 非表示は、編集ペインの入力項目として変更しない。位置はノートキャンバス上のドラッグ移動により変更し、表示 / 非表示はコンテキストメニュー右上の `wilt`、`bloom`、`root`、`hide` 操作により変更する。編集ペインは、ラベル、説明、形状、色、フォント、リソース権利情報などの属性編集を担当する。

Viewpoint axis や Timeline axis の軸線そのものは、通常 `links[]` に正本として保存せず、`groups[]` から実行時に生成される pseudo link として扱う。

== 共通レコード項目

Node、Link、Group、Resource などの保存対象レコードは、可能な限り次の共通項目を持つ。

[cols="1,3", options="header"]
|===
|項目 |意味

|`id`
|内部識別子。

|`state`
|状態。通常は `active`、削除時は `deleted`。

|`deleted`
|削除履歴。`state: "deleted"` の場合に設定する。

|`visible`
|通常描画対象かどうか。`hide` / `bloom` / `wilt` / `root` などの表示制御で変更される。編集ペインでは変更しない。

|`description`
|説明本文。基本は `{format, body}` の object。

|`audit`
|作成・更新情報。
|===

=== state と deleted

削除は物理削除せず、`state: "deleted"` として保存する。

[source,json]
----
{
  "state": "deleted",
  "deleted": {
    "deletedBy": "user_uuid",
    "deletedAt": "2026-05-21T10:30:00+09:00",
    "reason": "user operation"
  }
}
----

通常描画では `state: "deleted"` の要素を除外する。履歴表示、復元、差分確認では参照可能とする。

=== audit

[source,json]
----
{
  "owner": "guest",
  "createdBy": "user_uuid",
  "createdAt": "2026-05-21T10:00:00+09:00",
  "lastModifiedBy": "user_uuid",
  "lastModifiedAt": "2026-05-21T10:30:00+09:00"
}
----

`createdBy` と `createdAt` は作成履歴として保持し、原則として変更しない。

NOTE: `resource.storage.files[].path`、`resource.storage.manifest.path` などの内部ファイル保存項目には、`http://` 又は `https://` のホスト名、`/wu_wei2/server/load-file.cgi`、`/wu_wei2/cgi-bin/load-file.py`、`user_id` クエリ、利用者 UUID を含めない。保存値は、`YYYY/MM/DD/file_uuid/filename` 形式の論理パスとする。remote Content の外部 URL は `resource.original.url` に保存し、表示時に必要な `load-file` URL は実行時に生成する。


== Description

通常の `description` は、次の object 形式を基本とする。

[source,json]
----
{
  "format": "asciidoc",
  "body": "説明本文"
}
----

`format` は主に次の値を想定する。

* `plain`
* `asciidoc`

NOTE: `description` の配列化は、全ノート・全ノードに対して一律に行わない。import 済みノートにおいて、他者作成ノードへ補足説明を追記する必要がある場合に限定して、補足 entry の追加構造を導入する。

共同編集モードでは、他者作成要素の `description` を直接追記・変更しない。必要な修正提案は Memo で伝える。

== Node

=== 共通項目

[source,json]
----
{
  "id": "_node_uuid",
  "type": "Topic",
  "label": "表示ラベル",
  "description": {
    "format": "asciidoc",
    "body": ""
  },
  "x": 100,
  "y": 100,
  "state": "active",
  "visible": true,
  "shape": "RECTANGLE",
  "size": {
    "width": 160,
    "height": 60
  },
  "linkCount": 0,
  "style": {
    "fill": "#ffffff",
    "font": {
      "family": "sans-serif",
      "size": 14,
      "color": "#303030",
      "align": "center"
    },
    "line": {
      "kind": "SOLID",
      "color": "#4c6b8a",
      "width": 1
    },
    "label": {
      "lines": 1,
      "offset": {
        "x": 0,
        "y": 0
      }
    }
  },
  "audit": {}
}
----

NOTE: `label` はすべてのノードの必須共通項目ではない。Memo は `label` を持たず、表示本文は `description.body` として定義する。

`color`、`outline`、`outlineWidth`、`font` は保存対象ではない。これらは `style` から実行時に展開される描画用 mirror field であり、保存前に削除する。`linkCount` は Node 直下の保存項目として残すが、正本は `links[]` であるため、load 時又は link 更新時に再計算してよい。

=== type

主な `type` は次のとおりである。

[cols="1,3", options="header"]
|===
|type
|意味

|`Topic`
|通常の概念ノード。グループ代表ノードも `type` は `Topic` とし、`topicKind`, `groupRole`, `representativeOf` により代表ノードであることを示す。

|`Content`
|PDF、Web、画像、動画、Office 文書などを表すノード。

|`Memo`
|付箋・メモ用ノード。

|`Segment`
|Timeline group のメンバー。

|`PageMarker`
|Viewpoint group のメンバー。
|===


=== Content node

Content node は、PDF、Office 文書、Web ページ、画像、動画、音声、アップロード済みファイルなどのコンテンツを表す。

Content node は通常ノードとして `label` 及び `description` を持つ。`label` は Content node の表示名であり、Resource 側には `title` を持たせない。`description` は、`description.format` 及び `description.body` により定義する。

現在の通常保存では、`resourceRef` ではなく、Content node 内の `resource` に compact resource を埋め込む。この `resource` は、<<resource-v2-basic>>で定義した v2 Resource 構造と一致する。`resourceView` も Content node の表示状態を表す保存対象項目である。

[source,json]
----
{
  "id": "_content_node_uuid",
  "type": "Content",
  "shape": "THUMBNAIL",
  "label": "document.pdf",
  "description": {
    "format": "asciidoc",
    "body": "=== 表題\n解説文 1\n解説文 2"
  },
  "x": 100,
  "y": 100,
  "state": "active",
  "visible": true,
  "size": {
    "width": 160,
    "height": 120
  },
  "linkCount": 0,
  "resourceView": {
    "mode": "default"
  },
  "resource": {
    "id": "949127a0-3f14-4ba6-9239-c471334fdbba",
    "source": "upload",
    "kind": "document",
    "documentKind": "pdf",
    "videoKind": "",
    "audioKind": "",
    "imageKind": "",
    "mimeType": "application/pdf",

    "original": {
      "type": "upload",
      "storageRole": "original"
    },

    "media": {
      "kind": "document",
      "mimeType": "application/pdf",
      "downloadable": true,
      "pageCount": 27
    },

    "contents": {
      "type": "pdf",
      "axis": {
        "unit": "page",
        "nodeType": "page"
      },
      "pageCount": 27,
      "hasPageCount": true,
      "pageOffset": 0,
      "firstPageNumber": 1,
      "sourceRole": "original"
    },

    "storage": {
      "managed": true,
      "copyPolicy": "reference",
      "manifest": {
        "area": "upload",
        "path": "2026/05/21/949127a0-3f14-4ba6-9239-c471334fdbba/manifest.json"
      },
      "files": [
        {
          "role": "original",
          "area": "upload",
          "path": "2026/05/21/949127a0-3f14-4ba6-9239-c471334fdbba/document.pdf",
          "dir_name": "data/{user_uuid}/upload/2026/05/21/949127a0-3f14-4ba6-9239-c471334fdbba/",
          "file_name": "document.pdf",
          "mimeType": "application/pdf",
          "size": 1234567,
          "sha256": "",
          "registeredAt": "2026-05-21T10:00:00+09:00",
          "registeredBy": "user_uuid"
        },
        {
          "role": "thumbnail",
          "area": "upload",
          "path": "2026/05/21/949127a0-3f14-4ba6-9239-c471334fdbba/thumbnail.jpg",
          "dir_name": "data/{user_uuid}/upload/2026/05/21/949127a0-3f14-4ba6-9239-c471334fdbba/",
          "file_name": "thumbnail.jpg",
          "mimeType": "image/jpeg",
          "size": 12345,
          "sha256": "",
          "generated": true,
          "registeredAt": "2026-05-21T10:00:00+09:00",
          "registeredBy": "server"
        }
      ]
    },

    "viewer": {
      "defaultMode": "pdfjs",
      "supportedModes": ["pdfjs", "newTab", "download"],
      "embed": {
        "enabled": true,
        "sourceRole": "original"
      },
      "thumbnailRole": "thumbnail"
    },

    "rights": {
      "owner": "",
      "copyright": "",
      "license": "",
      "attribution": ""
    },

    "state": "active",
    "audit": {}
  },
  "style": {
    "fill": "#ffffff",
    "font": {
      "family": "sans-serif",
      "size": 14,
      "color": "#303030",
      "align": "center"
    },
    "line": {
      "kind": "SOLID",
      "color": "#4c6b8a",
      "width": 1
    }
  },
  "audit": {}
}
----

`description` は Content node 自身の説明である。これは、Content node がノートキャンバス上でどのような意味を持つかを記述する。SVG 描画時には、`description.body` を `description.format` に従って HTML 化し、`foreignObject` 内に描画できる。

一方、`resource.original`、`resource.mimeType`、`resource.media`、`resource.storage`、`resource.viewer`、`resource.rights` は、Content node が参照する資料又はメディアの属性である。Content node の説明と、resource のメタデータは区別する。

Content node の `resource` には、サーバーがファイル登録時に確定した参照情報及び管理情報を含める。これらは保存データとして保持し、情報ペイン又は管理者用ペインで表示する。upload Content の `resource.storage.files[]` は通常の編集ペインでは変更させない。

新規 v2 Resource では、`resource.title`、`resource.uri`、`resource.canonicalUri`、`resource.thumbnailUri` は使用しない。Content の表示名は `node.label` に一本化し、アップロード実体は `resource.storage.files[]`、remote 参照情報は `resource.original.url` 及び `resource.original.canonicalUrl` により管理する。

`resourceRef`、node 直下の `uri`、`url`、`format`、`thumbnail` などの旧項目は、読み込み時の正規化段階で v2 Resource に変換する。正規化後の通常処理では、これら旧項目への fallback は行わない。
=== Memo node

[source,json]
----
{
  "id": "_memo_uuid",
  "type": "Memo",
  "shape": "MEMO",
  "description": {
    "format": "asciidoc",
    "body": "メモ本文"
  },
  "style": {
    "memo": {
      "corner": "bottom-right",
      "foldSize": 32
    }
  }
}
----

Memo は `label` を持たない。表示本文は `description.body` として定義する。
読み込み時又は保存時に Memo に `label` が存在する場合は、共通 Node 正規化処理等により付与された不要項目とみなし、無視する。
Memo の `style.label` も保存対象に含めない。

=== Segment node

Timeline axis 上の参照点は `Segment` ノードである。

[source,json]
----
{
  "id": "_segment_uuid",
  "type": "Segment",
  "topicKind": "timeline-point",
  "groupRef": "_timeline_group_uuid",
  "mediaRef": "_content_node_uuid",
  "mediaStart": 120,
  "mediaEnd": 180,
  "playDuration": 60,
  "time": {
    "start": 120,
    "end": 180
  },
  "axisPos": 120
}
----

=== PageMarker node

Viewpoint axis 上のページ参照ノードは `PageMarker` である。

[source,json]
----
{
  "id": "_page_marker_uuid",
  "type": "PageMarker",
  "topicKind": "viewpoint-page",
  "groupRef": "_viewpoint_group_uuid",
  "viewpointRef": "_viewpoint_group_uuid",
  "documentRef": "_content_node_uuid",
  "mediaRef": "_content_node_uuid",
  "axisRole": "entry",
  "pageNumber": 1,
  "anchorHref": "",
  "htmlAnchorHref": "",
  "label": "p.1",
  "x": 100,
  "y": 100,
  "shape": "CIRCLE",
  "size": {
    "radius": 18
  },
  "axisPos": 100,
  "visible": true,
  "audit": {}
}
----

PDF / Office 文書では `pageNumber` を使用する。HTML 文書では `anchorHref` 又は `htmlAnchorHref` を使用する。

`anchorHref` 及び `htmlAnchorHref` は、値が空の場合も PageMarker の保存対象項目として保持できる。PDF / Office 文書では通常は空文字列となり、HTML 文書ではアンカー値を持つ。

旧項目 `pageSequenceNumber`、`sequenceNumber`、`physicalPageNumber` は使用しない。

viewer page は次で求める。

[source,text]
----
viewerPage = pageNumber - content.resource.viewpoint.pageOffset
----

ただし、`viewerPage` の最小値は 1 とする。

=== グループ代表ノード

グループ代表ノードは通常の `Topic` に近いノードだが、グループとの関係を示す項目を持つ。

[source,json]
----
{
  "id": "_representative_node_uuid",
  "type": "Topic",
  "topicKind": "group-representative",
  "groupRef": "_group_uuid",
  "groupRole": "representative",
  "representativeOf": {
    "kind": "group",
    "id": "_group_uuid"
  },
  "representativeType": "horizontal",
  "label": "Group",
  "description": {
    "format": "asciidoc",
    "body": ""
  },
  "x": 100,
  "y": 100,
  "axisPos": 100,
  "state": "active",
  "visible": true,
  "shape": "RECTANGLE",
  "size": {
    "width": 120,
    "height": 32
  },
  "linkCount": 0,
  "style": {
    "fill": "#ffffff",
    "font": {
      "family": "sans-serif",
      "size": 14,
      "color": "#303030",
      "align": "center"
    },
    "line": {
      "kind": "DASHED",
      "color": "#4c6b8a",
      "width": 1
    },
    "label": {
      "lines": 1,
      "offset": {
        "x": 0,
        "y": 0
      }
    }
  },
  "audit": {}
}
----

グループ代表ノードは、通常の `Topic` と同じ保存項目を持つ。これに加えて、`topicKind`、`groupRef`、`groupRole`、`representativeOf`、`representativeType`、`axisPos` により、どの Group の代表ノードであり、どの軸上のどの位置に表示されるかを示す。`color`、`outline`、`outlineWidth`、`font` は `style` から展開される runtime mirror field であり保存対象に含めない。`changed`、`dragging`、`fx`、`fy` などの実行時項目も保存対象に含めない。

Timeline / Viewpoint の代表ノードでは、`topicKind` に次を使用する。

* `timeline-representative`
* `viewpoint-representative`
* `group-representative`

`representativeType` は原則として対象 Group の `type` と一致させる。通常グループでは `horizontal`、`vertical`、`simple`、Timeline group では `timeline`、Viewpoint group では `viewpoint` を使用する。

=== 保存用ページサムネイル

`note.thumbnail` 及び `page.thumbnail` に保存する SVG は、保存データであるため、実行時 DOM をそのまま複写してはならない。実行時 DOM には `load-file.cgi` / `load-file.py` URL、ホスト名、`user_id` パラメータが `<image href="...">` として含まれることがある。保存時のページサムネイルは、ページモデルから data-only SVG として生成し、利用者 UUID や実行時アクセス URLを含めない。

== Link

Link は通常ノード間の関係を表す。

[source,json]
----
{
  "id": "_link_uuid",
  "type": "Link",
  "from": "_source_node_uuid",
  "to": "_target_node_uuid",
  "relation": "",
  "label": "",
  "description": {
    "format": "plain",
    "body": ""
  },
  "x": 0,
  "y": 0,
  "shape": "NORMAL",
  "state": "active",
  "visible": true,
  "style": {
    "font": {
      "family": "sans-serif",
      "size": 14,
      "color": "#303030",
      "align": "center"
    },
    "line": {
      "kind": "SOLID",
      "color": "#c0c0c0",
      "width": 2
    }
  },
  "routing": {
    "path": ""
  },
  "audit": {}
}
----

実行時に `source` / `target` がオブジェクト化される場合があるが、保存時には `from` / `to` の ID 文字列へ正規化する。

=== 所有者混在 Link

共同作業モードでは、他者が作成したノードと自分が作成したノードを結ぶ Link を作成可能とする。この Link の所有者は Link を作成した利用者である。

[source,text]
----
他者作成 Node A  ── 自分作成 Link ── 自分作成 Node B
----

ただし、`from` 又は `to` のどちらかの端点ノードが `state: "deleted"` になった場合、この Link も `state: "deleted"` として履歴管理する。

== Group

`groups[]` は、グループ、軸、疑似描画要素生成の正本である。

[cols="1,2,3", options="header"]
|===
|type |groupType |説明

|`simple`
|
|矩形などの単純グループ。

|`horizontal`
|`axis`
|横方向の topic group。

|`vertical`
|`axis`
|縦方向の topic group。

|`timeline`
|`axis`
|動画や音声の時間軸。

|`viewpoint`
|`axis`
|PDF、Office preview PDF、HTML 文書などの Viewpoint axis。
|===

=== Group 共通項目

[source,json]
----
{
  "id": "_group_uuid",
  "name": "Group",
  "type": "horizontal",
  "groupType": "axis",
  "description": {
    "format": "plain",
    "body": ""
  },
  "state": "active",
  "visible": true,
  "moveTogether": true,
  "orientation": "horizontal",
  "spine": {
    "kind": "SOLID",
    "color": "#4c6b8a",
    "width": 4,
    "padding": 12,
    "paddingTop": 12,
    "paddingRight": 12,
    "paddingBottom": 12,
    "paddingLeft": 12,
    "visible": true
  },
  "members": [
    {
      "nodeId": "_node_uuid",
      "order": 1,
      "role": "member"
    }
  ],
  "axis": {
    "mode": "",
    "unit": "",
    "nodeType": "",
    "anchor": {
      "x": 0,
      "y": 0
    }
  },
  "origin": {
    "x": 0,
    "y": 0
  },
  "representativeNodeId": "_representative_node_uuid",
  "audit": {}
}
----

=== Viewpoint group

[source,json]
----
{
  "id": "_viewpoint_group_uuid",
  "name": "Viewpoint",
  "type": "viewpoint",
  "groupType": "axis",
  "visible": true,
  "moveTogether": true,
  "orientation": "horizontal",
  "documentRef": "_content_node_uuid",
  "mediaRef": "_content_node_uuid",
  "pageCount": 20,
  "documentPageCount": 20,
  "hasPageCount": true,
  "length": 400,
  "spine": {
    "kind": "SOLID",
    "color": "#4c6b8a",
    "width": 4,
    "padding": 12,
    "visible": true
  },
  "axis": {
    "mode": "document",
    "unit": "page",
    "nodeType": "page",
    "start": 1,
    "end": 20,
    "anchor": {
      "x": 0,
      "y": 0
    }
  },
  "members": [
    {
      "nodeId": "_page_marker_uuid",
      "order": 1,
      "role": "member"
    }
  ],
  "entries": [
    {
      "role": "entry",
      "nodeId": "_page_marker_uuid",
      "order": 1,
      "pageNumber": 1,
      "label": "p.1",
      "comment": ""
    }
  ]
}
----

Viewpoint group の `name` 及び `type` は構造識別子であり、旧 `Contents` group は `wuwei.note.v2` の読み込み時に `Viewpoint` / `viewpoint` へ正規化する。ただし、`group.label`、代表ノードの `label`、PageMarker の `label` は利用者定義の表示情報であるため、`Contents` という表示ラベルであっても自動的に `Viewpoint` へ書き換えない。

HTML Viewpoint の場合は、`axis.mode` を `html`、`axis.unit` を `anchor`、`axis.nodeType` を `pageMarker` とし、`entries[]` に `anchorHref` / `htmlAnchorHref` を保持する。

=== Timeline group

[source,json]
----
{
  "id": "_timeline_group_uuid",
  "name": "Timeline",
  "type": "timeline",
  "groupType": "axis",
  "mediaRef": "_content_node_uuid",
  "visible": true,
  "moveTogether": true,
  "orientation": "horizontal",
  "timeline": {
    "unit": "second",
    "start": 0,
    "end": 600,
    "mediaRef": "_content_node_uuid",
    "defaultPlayDuration": 15
  },
  "axis": {
    "mode": "time",
    "unit": "time",
    "nodeType": "segment",
    "start": 0,
    "end": 600,
    "anchor": {
      "x": 0,
      "y": 0
    }
  },
  "members": []
}
----

== 共同編集・import 編集ポリシー

=== モード判定

`wuwei.collab` は、ノートの状態を次の三値で判定する。

[cols="1,3", options="header"]
|===
|値 |編集方針の概要

|`own`
|自分で作ったノート。通常編集を許可する。

|`imported`
|他者から入手して import したノート。表示属性の調整や、自分が追加した要素の編集を中心とする。

|`team`
|チーム共同編集ノート。他者作成要素の変更・削除は不可。必要な指摘は Memo で伝える。
|===

=== 共同編集モードでの編集制約

共同編集モードでは、他者の作成した要素の変更及び削除は不可とする。どうしても変更が必要な場合は、対象要素に関連する Memo を作成して伝える。

[source,text]
----
他者作成要素を修正したい
  ↓
直接変更又は削除しない
  ↓
Memo を追加して修正提案を伝える
----

== チーム管理テーブル

=== teams.json

[source,json]
----
[
  {
    "team_id": "t-5c1f594a-315b-4bab-9075-62b1bebed7eb",
    "teamName": "teamX",
    "description": "共同作業チーム",
    "status": "active",
    "audit": {
      "createdBy": "system_admin_user_uuid",
      "createdAt": "2026-05-21T10:00:00+09:00",
      "lastModifiedBy": "",
      "lastModifiedAt": ""
    }
  }
]
----

=== team-members.json

[source,json]
----
[
  {
    "team_id": "t-5c1f594a-315b-4bab-9075-62b1bebed7eb",
    "user_uuid": "user_uuid",
    "teamRole": "editor",
    "status": "active",
    "joinedAt": "2026-05-21T10:00:00+09:00"
  }
]
----

`teamRole` は次の値を想定する。

[cols="1,3", options="header"]
|===
|値 |意味

|`teamAdmin`
|チームノート管理者。初期実装ではメンバー所属変更はシステム管理者が行う。

|`editor`
|一般編集者。

|`viewer`
|閲覧利用者。
|===

=== users.json

[source,json]
----
[
  {
    "user_uuid": "user_uuid",
    "displayName": "User Name",
    "systemRole": "user",
    "status": "active"
  }
]
----

`systemRole` は `systemAdmin` と `user` を想定する。

== Export / Import

ノートの export は、保存済み `note.json` だけでなく、参照先のアップロード実体も含める。

ZIP の概念構造は次である。

[source,text]
----
export-manifest.json
note/YYYY/MM/DD/{note_uuid}/note.json
upload/YYYY/MM/DD/{file_uuid}/...
----

`export-manifest.json` の例:

[source,json]
----
{
  "format": "wuwei-note-export",
  "version": "1.1",
  "exportedAt": "2026-05-21T01:00:00Z",
  "package_uuid": "package_uuid",
  "note": {
    "note_uuid": "_note_uuid",
    "note_key": "2026/05/21/_note_uuid/note.json",
    "name": "Note title"
  },
  "resources": []
}
----

import では、ZIP 内の `upload/` と `note/` を現在利用者又はチームの保存領域に復元する。

== 保存時に除外する実行時項目

次の項目は、描画や D3 simulation の一時値であり、保存正本には含めない。

* pseudo node / pseudo link
* `vx`, `vy`
* `fx`, `fy`
* `index`
* `changed`
* `dragging`
* `filterout`
* `opacity`
* `expire`
* `checked`
* `transparency`
* `path`
* `_markerPath`
* `_markerTransform`
* hover、selection、dragging などの UI 状態
* computed path、computed label position などの再計算可能な値

ただし、ユーザーが明示的に調整した `routing.path` などの永続項目は保存対象にできる。

== 整合性チェックの要点

ホーム画面、検索、ノート読み込み、Viewpoint / Timeline 表示が正しく動作しない場合は、次を確認する。

* `note/YYYY/MM/DD/{note_uuid}/note.json` が存在するか。
* `list-note` 応答の `note_key` を使って対象ノートを指定しているか。
* `dataModel.name` が `wuwei.note`、`dataModel.version` が `v2` か。
* `pages[]`、`nodes[]`、`links[]`、`groups[]` が配列か。
* 各 Node / Link / Group に `id` があるか。
* `state: "deleted"` の要素を通常描画に出していないか。
* Content node の `resource.source`、`kind`、`documentKind`、`storage.files[]` が補完されているか。
* `storage.files[]` に必要な `role`、`area`、`path` があるか。
* `storage.manifest.path` が対応する `upload/.../manifest.json` を指しているか。
* PDF / Office の Viewpoint axis で `resource.viewpoint.pageCount` 又は group の `pageCount` が正しいか。
* PageMarker で `pageNumber` 又は HTML anchor が正しく設定されているか。
* Team note の場合、`team_id` が `teams.json` と `team-members.json` に登録されているか。


=== resource.viewpoint と legacy resource.contents

新規データでは `resource.contents` は使用しない。
Document / HTML Content のページ番号、anchor、viewer page 変換、PageMarker / Viewpoint に関係する metadata は `resource.viewpoint` に保存する。

旧 v2 以前のデータに `resource.contents` が存在する場合は、読み込み時又は v2 正規化時に `resource.viewpoint` へ移行し、保存時には `resource.contents` を出力しない。
